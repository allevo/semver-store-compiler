{
  "name": "semver-store-compiler",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "unit": "tap test.js",
    "lint": "standard",
    "test": "npm run lint && npm run unit",
    "coveralls": "npm run unit -- --cov"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "benchmark": "^2.1.4",
    "coveralls": "^3.0.2",
    "microtime": "^2.1.8",
    "pre-commit": "^1.2.2",
    "semver-store": "^0.3.0",
    "standard": "^12.0.1",
    "tap": "^12.0.1"
  },
  "dependencies": {
    "generate-function": "^2.3.1"
  }
}
